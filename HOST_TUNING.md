# 主机侧延迟调优（USB / EtherCAT）

> **文档类型**：现行规范（主机运行环境）
> **适用范围**：跑 librmcs host SDK 的上位机，USB bulk 与 EtherCAT/IgH 两种传输
> **状态**：现行有效（2026-08-04 在本机重测并修订；2026-08-01 版的部分结论已被推翻，见第 2 节）
> **相关文档**：[host-tuning.sh](host-tuning.sh)（可执行版本） · [host/examples/usb_ep0_rtt.cpp](host/examples/usb_ep0_rtt.cpp)（主机路径探针） · [firmware/rmcs_board/ecat/DESIGN.md](firmware/rmcs_board/ecat/DESIGN.md) 3.5/3.6/第 5 节（板级完整分布）

## 摘要

这份文档回答一个问题：**为了让 USB / EtherCAT 延迟达到板子本身的能力，上位机需要设置什么，
以及哪些设置其实没用。** 每一条都标了证据等级——`[实测]`、`[实测无效]`、`[未实测]`——因为这类
调优清单最容易变成"抄了一堆但没人验证过"的咒语。

**一句话结论：主机侧真正有用的只有两件事，而且各管一头。**
**管 p50 的是"不让核降频"**——做法是 `governor=performance`（而不是此前写的"关深 C-state"）。
**关深 C-state 之所以有效，是因为它顺带把频率顶住了，不是因为省下了 C-state 退出延迟**
——两者一起用并不比单用其中一个更好。
**管尾部的是"把事件线程绑到一个 P 核"**——注意是**绑核**，不是隔离核。
`isolcpus` 在纯主机路径上值 p99.9 约 -5~13us，但**叠上板+USB 往返后只剩 1~3us**，
而代价是一整个物理 P 核，所以本机最终**没有启用**，见 1.3。
**绝对不要不绑核**（p99.9 176us / max 458us），也不要绑到 E 核。

**它值多少，完全取决于你的循环让不让核空闲**：紧凑压测下是 0，1kHz 控制环下是
**板级 p50 16-18us**（120 -> 102，翻掉一整个微帧）。别拿压测结论去配置控制环，见 1.1 / 1.2。

> **但在动主机之前先确认固件是 `release` 构建。** 固件从 `debug`(-Og) 换成 `release`(-O3)
> 值 **20us 板级 p50**，比本文全部主机旋钮加起来还大，而且不需要任何运行期配置。见 8.4。

**除内核 cmdline 外全部不持久化，每次重启都要重跑。**

```bash
sudo ./host-tuning.sh          # 应用所有一次性设置并报告本机发现
sudo ./host-tuning.sh --check  # 只报告，不改
sudo ./host-tuning.sh --pmqos  # 另开终端，测量期间持住（只为最后约 2us 的 p99）
```

## 本文导航

- 第 0 节：**两个测量仪器，不要混用**——很多"矛盾的结论"其实是用错了尺子
- 第 1 节：真正有效的两条（顶住频率、隔离物理核），含推翻旧结论的对照数据
  - **1.1 / 1.2：顶住频率只对"核会空闲"的负载有用——但那正是控制环，值 16-18us 的板级 p50**
  - **1.3：绑核到 P 核是必须的；`isolcpus` 在板级只值 1~3us，不抵一个物理核，本机已弃用**；
    含 P/E 核 2x2 对照，以及一个坑：隔离核上 `scaling_cur_freq` 恒读最低频，会把频率类结论读反
- 第 2 节：**实测无效或有害的做法**（别再加回来），本节价值不低于第 1 节
  - **2.1：`performance` 只设给隔离核会赔 8-9us 的 p99**——中断线程和主线程在管家核上
  - **2.2：xHCI 中断线程落在 E 核上要赔约 21us 的 p50**——本文最大的新发现之一
- 第 3 节：USB 侧逐项清单与证据等级
- 第 4 节：**本机事实**——会漂移，用脚本发现，别抄
- 第 5 节：**还没测过的**，按预期收益排序
- 第 6 节：EtherCAT 侧（**历史记录**：本机已无对应硬件）
- 第 7 节：USB 代码本身（host + device）还有没有余量
- 第 8 节：板端 C++ 全流程还有没有余量
  - **8.4：固件构建类型 `debug`(-Og) -> `release`(-O3) 值 20us 板级 p50——本文最大的一个旋钮**
  - **8.5：固件不在关键路径上**——流水线步进 51.7us vs CAN 线时 50.3us，板端余量只剩 1.4us
    - **8.5.1：固定下限的分布是双峰的（峰距约 14us），不是 125us 微帧格子**；这解释了为什么单轮 p50 不可比
- 第 9 节：多块 USB 板并发会互相拖累（决定多板方案选型）
  - **9.2：USB HS 包率上限单板约 56000 包/s，且是每设备的——加板子能叠加**
  - **9.3：CAN 吞吐上限是线速**（FD 19870 帧/s、classic 8560 帧/s），主机和固件都动不了；含协议只支持 8 字节负载的结构性限制
- 第 10 节：为什么两种传输对主机调优的敏感度不同

---

## 0. 两个测量仪器，不要混用

主机调优的结论会因为用哪把尺子而完全不同。仓库里有两个，**它们测的不是一件事**：

| | `can_loopback_latency` | `dual_board_test latency` | `usb_ep0_rtt` |
|---|---|---|---|
| 测什么 | 板+USB 完整 RTT | 同左，但**跨两块板的真实总线** | **只测主机路径**：提交 -> xHCI -> 设备 EP0 -> 中断 -> 唤醒 |
| 需要接线 | 单板 CAN0<->CAN1 回环线 + 两端 120 欧 | 两块板 CAN0<->CAN0、CAN1<->CAN1、UART0<->UART0 | 不需要，不需要固件配合 |
| **采样间隔** | **1ms**（核会进入空闲） | **无间隔**，紧凑 ping-pong 约 10kHz（核不空闲） | 可配，默认 1ms |
| 量化 | **被主机控制器对 NAK 中 bulk 端点的重试节奏量化到 125us** | 同左 | 不量化，连续 |
| 典型 p50 | 99.8（单核镜像）/ 124.8（核对调镜像） | 100.6 `[实测 2026-08-04]` | 67~78 |
| 该用它做什么 | 板级选型、固件对比 | **验证总线真的通**（两个独立控制器、独立时钟） | **A/B 主机内核设置** |

两把吞吐尺子，同样不能混用（2026-08-04 新增，见 9.2 / 9.3）：

| | `dual_board_test stress` | `usb_packet_rate` |
|---|---|---|
| 测什么 | **CAN 帧率**——每次迭代把 can0+can1 batch 进**同一个** USB 包 | **USB 包率**——每条记录各占一个包 |
| 它的上限是谁的 | **CAN 线速**（FD 19870 帧/s、classic 8560 帧/s） | **USB 路径**（单板约 56000 包/s，每设备独立） |
| 陷阱 | 包率恰好等于帧率，**两个上限被混在一起**，看起来像"USB 只能 19000" | flood 模式下 CAN 会大量丢帧，那是正常的：测的是包不是帧 |

> **这正是"用错尺子"的教科书例子**：只有 `stress` 时，19000 这个数既可以读成 CAN 上限
> 也可以读成 USB 上限，无从分辨。拆包之后才看出 USB 那边还有 3 倍余量。

> **`dual_board_test` 的采样间隔是 0，这不是细节，它会直接改变主机调优的结论**——见 1.1。

**关键推论：主机侧一个 10us 的改进，在 `can_loopback_latency` 的 p50 上通常完全看不见**
——因为那个 p50 被钉在微帧格子上，10us 不足以翻掉一整格。2026-08-01 版本文档判定
"governor 无效"，就是这么来的：**结论对那把尺子是对的，对主机路径是错的。** 见第 1 节。

> **另一个常被误当成"USB 协议下界"的点**：HS bulk 在协议上并不按微帧发放，bulk 走异步调度。
> 产生 125us 台阶的是**主机控制器对 NAK 中的 bulk 端点的重试限流**（EHCI 时代是 QH 里的
> `NakCnt`/`RL`，xHCI 藏进了控制器内部调度）。这不是文字游戏：它意味着 125us 是
> **这块控制器的策略**，换一块控制器未必一样——所以第 5 节把"换控制器重测"排在很前面。
> `[推断，未实测]`

---

## 1. 真正有效的两条：顶住频率（吃 p50）、隔离物理核（吃尾部）

`usb_ep0_rtt`，3000 样本、1ms 间隔、`chrt -f 80` 绑 cpu7，**连测 3 轮**，
每行右侧是运行期间实际采到的核心频率 `[实测 2026-08-04]`：

| 配置 | p50 | p99 | p99.9 | cpu7 频率（均值） |
|---|---|---|---|---|
| 基线（powersave，C-state 全开） | 77.4 / 77.5 / 77.8 | 95.9 / 100.9 / 101.0 | 110.8 / 121.8 / 125.8 | **1830 / 2141 / 2295 MHz** |
| `pm_qos=0`（关全部 idle 状态） | 68.3 / 68.3 / 68.2 | 80.9 / 79.3 / 80.0 | 90.2 / 86.5 / 87.0 | 4088 / 4093 / 4097 MHz |
| **`governor=performance`** | **67.4 / 67.7 / 67.5** | 80.7 / 82.2 / 82.2 | 95.4 / 93.2 / 89.3 | 4141 / 4227 / 4104 MHz |
| 两者同时 | 68.2 / 68.3 / 68.3 | 78.2 / 79.9 / 80.1 | 84.9 / 87.0 / 91.7 | 4087 / 4094 / 4081 MHz |

**三件事，按重要性排：**

1. **两者不是叠加的。** 同时开并不比单开好（p50 68.3 vs 67.5）。**说明它们是同一个效应**：
   两条路都只是让核别进入让频率掉下去的状态。
2. **机制是频率驻留，不是 C-state 退出延迟。** 基线那一列的频率是决定性证据：核在
   **1.8-2.3 GHz** 上跑，而不是 4.1 GHz。这也解释了为什么 p50（不只是尾部）会动——
   退出延迟只会影响被唤醒的那一次，而降频影响整条处理路径。
3. **一个"会睡的"SCHED_FIFO 线程不会把频率顶住。** 1kHz 控制环里核有 99% 的时间是空闲的，
   powersave 下调频器看到的就是一个几乎全空闲的核。**注意这条有前提，见 1.1**——
   忙循环确实会把频率顶住，旧文档那句话在它自己的测量条件下是成立的。

**所以推荐做法变了**：用 `governor=performance`，它不需要常驻进程、不影响其他核的功耗，
拿到约 95% 的收益。`--pmqos` 只在追最后约 2us 的 p99 时再加。

> 细节：`pm_qos=0` 时频率被限在 4100 MHz（三轮 max 都是 4100），而 `performance` 能到
> 4500。但 p50 只差 0.8us——**这条链路对频率的敏感度在 4 GHz 以上已经饱和**。

### 1.1 前提：只有"核会空闲"的负载才吃这一条 [实测 2026-08-04]

上面的收益**有一个前提，而且这个前提决定了你要不要设 governor**。同一个探针，只改采样间隔：

| | `powersave` | `performance` |
|---|---|---|
| **间隔 1000us**（核在两次之间空闲） | p50 **77.9**，频率 **2294 MHz** | p50 **67.5**，频率 4325 MHz |
| **间隔 0**（紧凑循环，核一直忙） | p50 **63.7**，频率 **4353 MHz** | p50 63.4，频率 4338 MHz |

**紧凑循环下两者完全一样，而且 `powersave` 自己就跑在 4353 MHz。** 忙循环会把频率顶上去，
调频器根本没机会降频。**只有当核在两次事务之间真的空闲下来，`powersave` 才会掉到 2.3 GHz。**

这条规则解释了所有看起来矛盾的测量，并且**修正了我在第 1 节开头对旧文档的批评**：

- 2026-08-01 版写的"RT 线程本来就把频率顶住了"——**对忙循环是对的**，只对会睡的循环是错的。
  旧结论不是凭空来的，它的适用范围没写清楚而已。
- `dual_board_test latency` 默认是**无间隔**的紧凑 ping-pong，所以它测 governor 必然测不出差别。
  实测两轮对照，p50 100.6/100.6（powersave）vs 100.7/100.7（performance），
  min、p99 也全在噪声内 `[实测 2026-08-04，两块 5321 DualCan]`。**这是真实的零，不是没测出来。**

### 1.2 但在真实占空比下，它会翻掉一整个微帧 [实测 2026-08-04]

把 `dual_board_test latency` 的采样间隔设成 1000us（`RMCS_LATENCY_GAP_US=1000`，
即 1kHz 控制环的占空比），**同一个板级 RTT，结论完全反过来**：

| 主机配置（间隔 1000us，两块 5321 DualCan，各两轮） | min | p50 | p99 | max |
|---|---|---|---|---|
| `powersave` | 89.6 / 91.7 | **119.9 / 120.2** | 132.4 / 135.4 | 155.3 / 191.2 |
| `performance` | 77.7 / 77.6 | **101.7 / 104.1** | 124.4 / 125.4 | 134.0 / 141.4 |
| `powersave` + `--pmqos` | 78.5 / 86.1 | **103.8 / 104.6** | 124.6 / 124.2 | 155.6 / 142.7 |

**这一节推翻了第 10 节此前"主机调优改不了板级 p50"的说法。** 三件事：

1. **`performance` 在板级 p50 上值 16-18us**，不是"只改善尾部"。主机路径那 10us 足以让回复
   **错过当前微帧**，代价就是一整格。min 也差 12us、max 差约 50us。
2. **`--pmqos` 和 `governor` 可以互相替代**（103.8 vs 101.7），再次印证第 1 节
   "同一个效应"的结论——两者都只是把频率钉住。
3. **这几乎可以肯定就是 2026-08-01 那次"governor 无效"的成因**：那份文档要求
   "测 USB 延迟之前必须先开 `--pmqos`"，而 `--pmqos` 已经把频率钉住了，
   于是再切 governor 当然没有差别。`[推断]`——当时的具体条件无法复核，但与全部数据自洽。

> **判据：主机调优在板级 RTT 上有没有用，取决于你的循环让不让核空闲。**
> 紧凑压测（间隔 0）：零。1kHz 控制环（间隔 1000us）：**16-18us 的 p50**。
> **拿压测结论去配置控制环的主机，会白白丢掉一个微帧。**

**端到端验证** `[实测 2026-08-04]`：跑完 `sudo ./host-tuning.sh` 之后直接测：

```
after host-tuning.sh  n=2900 min=49.4 p50=67.5 p90=72.0 p99=77.1 p99.9=90.1 max=97.0 us | cpu7 4440/4505 MHz
```

对基线的 p50 77.5 / p99 ~100，**p50 -10us、p99 -23us**。

### 1.3 第二条有效的：把事件线程的物理核整个隔离（只吃尾部）[实测 2026-08-04]

**结论：`isolcpus` 从一个 E 核换成"一对 SMT 兄弟构成的物理 P 核"，p99.9 -5~13us、
max 和轮间离散度明显收窄，而 p50 一动不动、p99 只能说"不劣于改动前最好的一次"。**
判据：**SMT 抢占是尾部抖动源，不是稳态开销**——兄弟线程抢的是共享的 L1/L2 和执行单元，
只在它真的被调度到的那几次才咬一口，所以它动 p99.9 和 max，不动 p50。

改动（GRUB `GRUB_CMDLINE_LINUX`，重启生效）：

| | 改前 | 改后 |
|---|---|---|
| `isolcpus` / `nohz_full` / `rcu_nocbs` | `15` | `6,7` |
| `irqaffinity` | `0-14,16-21` | `0-5,8-21` |

改前的配置有两个毛病叠在一起：`cpu15` 是 E 核，**而且没有任何线程用它**；真正跑 USB
事件线程的 `cpu7` 根本没被隔离，它的 SMT 兄弟 `cpu6`（`cpu7/topology/thread_siblings_list`
= `6-7`）上可以跑任何东西。

`usb_ep0_rtt`，cpu7、3000 样本、1ms 间隔、`governor=performance`，各连测 3 轮 `[实测]`：

| `isolcpus` | p50 | p90 | p99 | p99.9 | max | cpu7 频率（均值） |
|---|---|---|---|---|---|---|
| `15`（E 核，cpu6 未隔离），1.1 那三轮 | 67.4 / 67.7 / 67.5 | — | 80.7 / 82.2 / 82.2 | 95.4 / 93.2 / 89.3 | — | 4141 / 4227 / 4104 |
| `15`，1.2 末尾的端到端那一次 | 67.5 | 72.0 | **77.1** | 90.1 | 97.0 | 4440 |
| **`6,7`（独占物理 P 核）** | 67.8 / 67.7 / 67.8 | 73.0 / 73.1 / 72.9 | **76.7 / 78.2 / 76.6** | **85.0 / 83.8 / 82.7** | **90.2 / 89.8 / 89.6** | 4282 / 4294 / 4284 |

**必须把改动前的两组数据一起看**，只挑 1.1 那三轮会把收益说大——1.2 末尾那次改动前就已经
跑出 p99=77.1，和改动后的 76.6-78.2 完全重叠。四件事：

1. **p50 没动**（67.8 vs 67.5，甚至高 0.4us，在噪声内）。这与第 1 节自洽：p50 被微帧格子
   钉着，只有频率那种"影响整条处理路径"的因素才翻得动一整格，偶发抢占翻不动。
   **改这一条之前预期的"p50 减少几 us"没有兑现，按实测修正。**
2. **p99 只能说"不劣于改动前最好的一次"。** 改动前四次落在 77.1-82.2，改动后三次落在
   76.6-78.2——分布整体下移且收窄，但区间有重叠，**单看 p99 不足以证明这次改动有效**。
   改动前那次 77.1 的 cpu7 频率是 4440 MHz，比其余几次都高，**频率在轮间的漂移足以解释
   这个量级的 p99 差异**，所以 p99 这一列是被混杂的。
3. **真正立得住的是 p99.9、max 和轮间离散度，它们不重叠。** p99.9 改动前 89.3-95.4（四次）
   、改动后 82.7-85.0（三次）；max 改动前记录到 97.0、改动后 89.6-90.2。p99.9 轮间跨度
   从 6.1us 降到 2.3us。**这说明抖动源是被移走了，而不是被平均掉了。**
4. **频率反而略高**（均值 4284 vs 4157）。独占物理核不与兄弟线程争 turbo 预算，方向合理；
   但这条链路对频率的敏感度在 4 GHz 以上已经饱和（见 1.1 末尾），所以它不是收益来源。

**吞吐这一侧它同样不动。** 同一探针把间隔设成 0，量主机路径的事务上限 `[实测 2026-08-04]`：

| `isolcpus=6,7`，间隔 0，20000 样本 | p50 | p99 | max | 实测速率 | cpu7 频率（均值） |
|---|---|---|---|---|---|
| 第 1 轮 | 63.9 | 70.9 | 85.6 | **15623 事务/s** | 4397 |
| 第 2 轮 | 63.9 | 71.9 | 95.2 | **15599 事务/s** | 4403 |

p50 63.9 与 1.1 表里改动前的 63.7/63.4 一致，**隔离对紧凑循环的吞吐上限没有影响**——
符合预期：紧凑循环下核一直忙，兄弟线程本来就抢不到多少，而且频率自己就顶到了 4.4 GHz。
**主机路径的事务上限就是约 15.6k/s（p50 倒数 15.6k，与墙钟一致），这是 EP0 控制传输的
数字，不是 bulk 吞吐**——bulk 的 MB/s 上限受 NAK 重试策略支配，是另一回事（第 0 节）。

> **隔离核必须显式绑上去，否则是净亏。** `isolcpus` 把 6/7 移出了通用调度池，所以**不绑核的
> 线程永远不会跑到隔离核上**，只会挤在剩下的核里。板级实测（8.4 那张表，`release` 固件、
> 两块 5321、20000 样本）：不绑核 p99.9 176.4 / max 458.0，绑到 7/6 是 129.4 / 161.0。
> **`dual_board_test` 的核参数默认是 `-1`（不绑），照默认跑等于白隔离还倒赔。**

> **在板级，隔离只值约 1.5us 的 p99——小，但不是零；而绑 P 核还是 E 核的差别大得多。**
> `release` 固件，四种绑核**交替**跑 3 轮 x 20000 样本 `[实测 2026-08-04]`。
> p50 在本机是双峰的（8.5.1），不可比，看 p99/p99.9/max：
>
> | 绑到哪 | p99 | p99.9 | max |
> |---|---|---|---|
> | **隔离 P 核 `7,6`** | **123.2 / 123.4 / 123.3** | **132.5 / 133.5 / 133.7** | 160.7 / 162.7 / 159.2 |
> | 普通 P 核 `8,9` | 125.2 / 125.4 / 124.0 | 134.1 / 134.3 / 134.7 | 160.4 / 151.8 / 159.0 |
> | E 核 `16,17` | 126.0 / 126.2 / 127.3 | 137.1 / 137.5 / 139.3 | **204.2 / 205.7 / 258.5** |
> | E 核 `20,21` | 128.3 / 130.6 / 134.1 | 140.1 / 146.3 / 147.3 | **161.0 / 218.8 / 325.5** |
>
> 上表的 E 核**未被隔离**，所以"E 核差"和"没隔离差"混在了一起。为了拆开，
> 临时把 grub 改成 `isolcpus=6,7,16,17`（一对 P 核 + 一对 E 核），重启后跑完整的
> **2x2 交替对照**，3 轮 x 20000 样本 `[实测 2026-08-04]`：
>
> | 绑到哪 | min | p99 | p99.9 |
> |---|---|---|---|
> | **隔离 P 核 `7,6`** | **75.3 / 76.0 / 75.8** | **122.2 / 122.3 / 122.1** | **127.2 / 128.1 / 128.5** |
> | 隔离 E 核 `16,17` | 76.2 / 79.0 / 79.0 | 123.1 / 123.1 / 123.3 | 128.8 / 133.3 / 132.9 |
> | 普通 P 核 `8,9` | 76.7 / 76.3 / 77.1 | 123.1 / 122.8 / 123.2 | 129.8 / 132.8 / 130.6 |
> | 普通 E 核 `20,21` | **86.6 / 84.5 / 86.0** | 124.1 / 123.6 / 123.4 | **138.5 / 137.1 / 136.5** |
>
> 1. **隔离对 E 核的价值远大于对 P 核。** E 核 min 从 84.5-86.6 降到 76.2-79.0
>    （**约 -8us**）；P 核只从 76.3-77.1 降到 75.3-76.0（**约 -1us**）。
>    E 核本来就承接更多通用调度，隔离把这部分挡掉了。
> 2. **所以"E 核差 2~10us"是被混杂的。** 隔离之后 P 与 E 的真实差距只有 **1~4us**
>    （min 75.3-76.0 对 76.2-79.0，p99.9 127.2-128.5 对 128.8-133.3）。
>    **P 核仍然更好，但没有此前看上去那么多。**
> 3. **隔离 P 核在三项上都是最优且最稳**（p99 三轮跨度仅 0.2us）。
>
> > **两次更正记录在此。** 本节最早写的是"每一列都完全重叠、隔离在板级不值钱"——错的，
> > 来自分两批测量、受轮间漂移污染。改成交替测量后得到"隔离值约 1.5us"，方向对了；
> > 但当时 E 核未隔离，于是又把"E 核差"说大了。**教训是同一个：配置必须交替跑，
> > 而且对照组的其他条件要一致。**
>
> **取舍**：1.5us 的 p99 换一整个物理 P 核（12 个 P 线程里的 2 个，约 17% 的 P 核算力）。
> 机器若还要跑别的计算任务，多半不划算。**注意 `isolcpus` 并不阻止显式绑核**，
> 所以第三条路是保留隔离，由计算任务自己用 `taskset` 在控制环空闲时去蹭那两个核。

> **`irqaffinity=` 保不住隔离核，它只设开机默认值。** 多队列驱动（nvme、iwlwifi、igc…）
> 会在运行期调 `irq_set_affinity_hint` 把队列按 CPU 铺开，直接落回隔离核。
> **实测 2026-08-04**：`isolcpus=6,7` + `irqaffinity=0-5,8-21` 都已生效的情况下，
> `nvme0q7` 和 `iwlwifi:queue_7` 仍钉在 cpu6、`nvme0q8` 和 `iwlwifi:queue_8` 钉在 cpu7
> ——**磁盘和无线中断压在 USB 事件线程那个核上**，笔记本上最突发的两个源。
>
> - **iwlwifi 的可以移走**：`echo <housekeeping> > /proc/irq/<n>/smp_affinity_list`。
> - **nvme 的移不走**：blk-mq 的 per-CPU 托管中断，`smp_affinity` 对它们只读，
>   只有在 probe 阶段减少队列数才能避开。
>
> `host-tuning.sh` 现在会枚举所有 IRQ、报出落在隔离核上的、能移的自动移到非隔离 P 核，
> 并把内核托管移不走的单独警告出来。
> **注意收益未能实测证实**：改动前后 p50 分别是 115.8 与 101.0/101.3/115.8，
> 而 p50 在本机本就在微帧格子间双峰跳变（见下），单次对照不足以定论；
> 同期机器上还有远程桌面进程在跑。**修它的理由是配置本身错了，不是测出了收益。**

> **不要把 xHCI 中断也挪进 6/7。** `irqaffinity=0-5,8-21` 是有意把中断挡在隔离核之外，
> 中断和事件线程同核正是第 2 节已经证伪的方案。

> **副作用（会让后续测量读反）：隔离之后 `scaling_cur_freq` 会骗人。** 内核不会为了满足
> 一次 sysfs 读就去 IPI 一个 `nohz_full` 的核采 APERF/MPERF，读数于是退化成
> `cpuinfo_min_freq`：`cpu7` 恒读 **400 MHz**，而同期 `turbostat` 测得 4259-4349 MHz。
> 频率列是第 1 节全部结论的证据来源，读成 400 MHz 会把结论整个读反。
> `usb_ep0_rtt` 已改为直接从 `/dev/cpu/<n>/msr` 读 APERF/MPERF 自行换算（`CpuClock`），
> sysfs 只留作降级路径。**其他还在读 `scaling_cur_freq` 的脚本，在本机对隔离核已不可信。**

### 1.4 旧结论：关深 C-state 对板级 RTT 的影响（仍然有效，但要重新理解）

2026-08-01 用 `can_loopback_latency`（核对调镜像）测的，只切 PM QoS 这一个变量 `[实测]`：

| | 默认 | PM QoS = 0 |
|---|---|---|
| p50 | 124.8 | 124.8（不变） |
| p99 | 150.8 | **146.0** |
| p99.9 | 188.2 | **158.9** |
| max | 216.8 | **160.7** |

这组数**没有被推翻**，但按第 1 节要重新理解：起作用的多半仍是频率，而 p50 纹丝不动是因为
它被微帧格子钉住（第 0 节）。**结论不变：测 USB 延迟之前必须先把主机弄到高频状态，
否则测到的是电源管理，不是传输。**

---

## 2. 实测无效或有害的做法

**这一节的价值不亚于第 1 节**——它们都是看起来合理、抄起来顺手的做法。

| 做法 | 预期 | 实测 | 结论 |
|---|---|---|---|
| **只在应用核上禁 C6/C10** | 精准替代全机 PM QoS | p50 77.8 / 77.1，与基线 77.4 / 77.5 无差别 | **无效** `[实测 2026-08-04]`。中断线程在别的核上，且 C1E 仍让频率掉下去 |
| **全核禁 C6/C10、保留 C1E**（等价于 `pm_qos=2`） | 拿到 PM QoS 的收益又省电 | p50 77.0 / 77.0（**没动**），p99 93.5 / 93.6（略好） | **只买到一点尾部，买不到 p50** `[实测 2026-08-04]`。C1E 一项就足以让频率落下来 |
| cpuidle governor `menu` -> `teo` | 更适合周期性唤醒 | p50 77.9 / 77.8，p99 95.6 / 93.9 | **无差别** `[实测 2026-08-04]` |
| 拔掉同 root hub 上的蓝牙（btusb） | 它的周期性分裂事务挤占微帧头部 | p50 77.7 / 77.0，p99 99.8 / 100.2 | **无差别** `[实测 2026-08-04]`。注：这是在 EP0 探针上测的，对 bulk 是否同样无关未验证 |
| CPU governor `powersave` -> `performance` | 降低尾部 | **见第 1 节：有效，约 10us** | **旧版"无效"的结论已推翻** |
| xHCI 中断线程提到 FIFO 90（**只接一块板时**） | 中断优先被处理 | 三轮无差别 | **无效**——但**只在单板时成立**，多板时结论相反，见第 9 节 |
| **把 xHCI 中断和 libusb 事件线程绑同一个核** | 省掉跨核唤醒 | **p50 124.8 -> 146.2（劣化 21us）**，p90 126 -> 157 | **有害，绝对不要做**。ISR 与事件线程在一个核上串行，损失远大于跨核唤醒的收益 |

**xHCI 中断要和 libusb 事件线程分开放**，这是本机默认行为，不要"优化"它。

---

### 2.1 把 `performance` 只设给隔离核——省电但赔尾部 [实测有害 2026-08-04]

看起来很合理：既然只有事件线程跑在 cpu6/7，把 `performance` 只给这两个核，其余留
`powersave`，就能拿到延迟又不用全机烧电。**实测赔 8-9us 的 p99。**

`dual_board_test latency 0 20000`，间隔 1000us，绑核 7/6，各两轮 `[实测]`：

| governor 范围 | min | p50 | p99 | p99.9 | avg | max |
|---|---|---|---|---|---|---|
| **全核 `performance`** | 84.5 / 82.2 | 115.9 / 116.8 | **123.2 / 124.4** | **134.0 / 134.8** | **110.1 / 111.2** | 160.9 / 155.9 |
| 仅 cpu6/7 `performance` | 84.5 / 84.0 | 116.5 / 116.4 | **132.3 / 132.5** | **143.3 / 141.9** | **116.5 / 116.1** | 201.1 / 164.5 |

**min 和 p50 完全一样，尾部整体劣化。** 原因是**关键路径不止事件线程**：

- **xHCI 中断线程跑在非隔离核上**（`irqaffinity` 有意把它挡在隔离核之外，见第 2 节
  已证伪的同核方案），它降频就直接拖慢每一次完成通知；
- **应用主线程也在管家核上**。

**判据：governor 的范围要覆盖整条路径，不是只覆盖你绑核的那个线程。**

> 顺带澄清一个看起来吓人但不成立的怀疑：本机 `package_throttle_count` 累计到 9779，
> 一度让人以为全核 `performance` 在热节流。**实测期间它不涨**——四轮 20000 样本的
> 测量里只增加了 0/1/0/0 次，那些累计值来自本会话跑的编译和 clang-tidy。
> **全核 `performance` 在这个负载下不产生热节流。** `[实测]`

---

### 2.2 xHCI 中断线程落在 E 核上：约 21us 的 p50 [实测 2026-08-04]

**结论：中断线程在哪个核上，比它的优先级更值钱。** 此前只测过"中断和事件线程同核"
（有害，见第 2 节），**从没人查过中断线程本身是在 P 核还是 E 核**。

A/B/A，两块 5321 DualCan、`release` 固件、事件线程绑 `7,6`、各 20000 样本 `[实测]`：

| xHCI 中断线程所在核 | min | p50 | p99 | p99.9 | avg |
|---|---|---|---|---|---|
| E 核 `cpu20` | 83.2 | **121.4** | 124.8 | 130.1 | **120.7** |
| **P 核 `cpu2`** | **77.0** | **100.3** | 122.9 | 131.2 | **103.4** |
| **P 核 `cpu2`** | **76.3** | **100.4** | 123.0 | 129.0 | **102.1** |
| E 核 `cpu20`（移回） | 82.5 | **121.4** | 125.5 | 140.2 | **120.6** |

**p50 差 21us、avg 差 18us、min 差 6us，移回去完整复现。** 比 `governor` 的 16-18us 还大，
与换 `release` 固件的 20us 相当。

**它是怎么冒出来的**：本机把 `irqaffinity=` 从 cmdline 去掉之后（1.3 的取舍），
内核就可以自由挑核，于是把 xHCI 中断丢到了 E 核。**这也是一次"回归"的真正原因**——
去掉隔离后 min 从 76.7 涨到 82.7，一度被误以为是 `isolcpus` 的锅，
但那次对照里 `cpu8,9` 本来就不在隔离列表内，`isolcpus` 解释不了它。

**跑这条链路的到底是哪些核**（最终配置，`[实测]` 逐核中断计数核对过）：

| 线程 | 所在核 | 角色 |
|---|---|---|
| USB 事件线程（libusb，每块板一个） | **cpu6 / cpu7，P 核** | 应用显式绑核，跑协议栈与完成回调 |
| xHCI 硬件中断线程 `irq/<n>-xhci_hcd` | **cpu0，P 核** | 接中断、唤醒事件线程 |
| 应用主线程 | 管家核，未绑 | 发包节流 |

> **`smp_affinity_list` 写的是"允许集合"，`effective_affinity_list` 才是真正生效的那一个核。**
> MSI 向量在硬件上只能指向**一个** APIC id，所以往 `smp_affinity_list` 写一个集合
> （这里是 P 核全集 `0-11`）之后，内核会从中**选出一个**——本机选中 `cpu0`，
> `/proc/interrupts` 里 IRQ 152 的 16237 次中断也确实全部落在 cpu0。
>
> **不要用 `ps` 的 `psr` 判断。** 那只是线程"上次跑在哪"，刚改完亲和性、线程还没被唤醒时
> 它仍显示旧核——据此会在中断其实还在 E 核上时报告成功。`host-tuning.sh` 因此读
> `effective_affinity_list`，只在读不到时才退回 `psr`。

**再细分：真正的问题不是"E 核"，是"LP E 核"。** 本机有**三档**核，而内核的
`cpu_atom`（12-21）把后两档混为一谈 `[实测 cpuinfo_max_freq]`：

| 档 | CPU | 最大频率 |
|---|---|---|
| P 核 | `0-11` | **4500 MHz** |
| 普通 E 核 | `12-19` | 3800 MHz（-16%） |
| **LP E 核**（SoC tile） | **`20-21`** | **2500 MHz（-44%）** |

把中断分别钉到三档上，各两轮 x 20000 `[实测]`：

| 中断线程所在 | min | p50 | avg |
|---|---|---|---|
| P 核 `cpu2` 4500MHz | 77.1 / 76.5 | **100.5 / 100.4** | 103.7 / 102.0 |
| 普通 E 核 `cpu16` 3800MHz | 77.4 / 78.7 | **100.8 / 119.2** | 102.9 / 111.3 |
| LP E 核 `cpu20` 2500MHz | 83.3 / 82.5 | **121.6 / 121.3** | 120.8 / 120.7 |

**LP E 核才是那 21us 的来源**；普通 E 核的 min 只差 1-2us，但 p50 一轮 100.8、一轮 119.2
——**正好卡在门槛上**。前面 2.2 表里那次"E 核"就是 `cpu20`，即最慢的一档。

> **这解释了 8.5.1 的双峰。** 8.5.1 测出固定下限的分布是双峰、峰距约 14-20us，
> 但当时未能查明成因。三档对照给出了答案：**中断被处理得够不够快，决定落进哪一簇。**
> 4500MHz 的 P 核总赶得上当前那次 USB IN 机会；2500MHz 的 LP E 核永远赶不上；
> 3800MHz 的普通 E 核在门槛上来回跳。**双峰不是玄学，是一个阈值现象。**

> **判据：xHCI 中断线程必须在 P 核，且不能和事件线程同核。** 两个条件都要满足——
> 同核那条是第 2 节已证伪的方案，E 核这条是本节。
> `host-tuning.sh` 现在会读出该线程实际所在的核，落在 E 核就把 IRQ 的
> `smp_affinity_list` 改到 P 核集合；`--check` 模式只报告（检测只需读权限，
> 所以不带 sudo 也能诊断）。

---

## 3. USB 侧逐项清单

| 项 | 本机状态 | 证据 | 持久化 |
|---|---|---|---|
| **`governor=performance`** | 需手动 | **实测有效，p50 上收益最大**（第 1 节） | 否 |
| **`isolcpus=6,7`（物理 P 核 + SMT 兄弟）** | 已设 | **实测有效，但只吃尾部**：p99.9 约 -5~13us、max 约 -7us；p50 不动，p99 证据不足（1.3） | **是**（GRUB cmdline） |
| 深 C-state 关闭（`--pmqos`） | 需手动 | 实测有效，但与上一条**重叠**，只多给约 2us 的 p99 | 否 |
| 设备 `power/control = on` | 本来就是 `on` | 内核对本设备的默认值即 `on`，未观察到 autosuspend | 是（默认） |
| root hub `power/control = on` | 原为 `auto`，已改 | **未实测出差别**（`runtime_status` 一直是 `active`）。消除一类可能性，不是修一个已知问题 | 否 |
| `usbcore.autosuspend` | `2`（秒） | 上面两条已经把这条路堵住了 | — |
| USB LPM（L1） | 本设备 sysfs 无此节点 | 硬件/驱动未暴露，无从设置 | — |
| `usbcore.usbfs_memory_mb` | `16` | **最有名的那个 USB 内核旋钮，和延迟无关**——它只是 usbfs 缓冲上限 | — |
| libusb IN 队列深度 | 16 个 transfer 常驻在飞 | **代码里已实现**（`kReceiveTransferCount`），不是系统设置 | 代码 |
| 事件线程绑核 + SCHED_FIFO | 每次测量 `chrt -f 80` + `thread_setup` | 必要。**但它不会替你把频率顶住**（第 1 节） | 否（按次） |
| xHCI 中断线程优先级 | FIFO 50（RT 内核默认） | 单板**实测无效**；**多板必需**（第 9 节） | 否 |
| irqbalance | 未运行 | 若运行会在运行时挪中断制造抖动，**保持关闭** | 是（服务 inactive） |

---

## 4. 本机事实：会漂移，用脚本发现，别抄这张表

下表是 2026-08-04 逐条读 sysfs/procfs 得到的 `[实测]`。**它一定会过期**——所以
`host-tuning.sh` 改成了运行时发现这些值并打印，写在这里只是给人一个参照。

| 项 | 当前值 | 2026-08-01 版文档写的 | 备注 |
|---|---|---|---|
| 内核 | `6.8.1-1056-realtime` | `6.8.1-1015-realtime` | — |
| CPU | Core Ultra 7 155H（Meteor Lake） | 未记 | P 核 `cpu0-11`、E 核 `cpu12-21`（`/sys/devices/cpu_core/cpus`、`cpu_atom/cpus`） |
| `isolcpus` | `6,7`，另有 `irqaffinity=0-5,8-21` | `7` | 2026-08-04 从 `15`（E 核，且无人使用）改成一对 SMT 兄弟组成的物理 P 核，**已实测，见 1.3** |
| `scaling_cur_freq` on `cpu6`/`cpu7` | **恒读 400 MHz（假值）** | 未记 | `nohz_full` 核不会被 IPI 采 APERF/MPERF；真值要用 `turbostat` 或 `/dev/cpu/<n>/msr`，见 1.3 |
| `sched_rt_runtime_us` | 开机后是 `950000` | 记为"已设 -1，不持久化" | 文档自己预言的回退**确实发生了**；`/etc/sysctl.d` 里没有相关配置 |
| 板子 | `a11c:a902` hpm5321_dual_can，bus 3，480M | 脚本只认 `a904` | **脚本因此一直报"board not enumerated"**，已修 |
| 板子的控制器 | `00:14.0`（PCH xHCI），IRQ **152** | 提到 `irq/135-xhci_hcd` | IRQ 号不跨重启稳定，已改为自动发现 |
| 另一个控制器 | `00:0d.0`（雷电 4 USB），**完全空闲** | 提到"本机有两个控制器" | 从未在单板 p50 上做过对照，见第 5 节 |
| 同 root hub 邻居 | btusb（Full Speed，走 root hub TT 分裂事务） | 未记 | 实测对主机路径无影响（第 2 节） |
| IOMMU | VT-d 开，`DMA-FQ`（惰性刷新） | 未记 | 见第 5 节 |
| 网卡 | `enp44s0`/`enp45s0`，均为 **r8169**（RTL8125） | `enp2s0`，`igc` | **本机没有 igc 网卡**，见第 6 节 |
| clang 工具 | `clang-format-18` / `clang-tidy-18` | `ENV.md` 的安装命令写 `-20` | 满足 `ENV.md` 表里的"≥16"，只是装的版本不同 |

---

## 5. 还没测过的，按预期收益排序

**这一节全部是 `[未实测]`。** 写在这里是为了不重复调研，不是为了照抄。

1. **把板子插到另一个 xHCI 控制器上。** 本机 `00:0d.0`（雷电 4 USB）一个设备都没有，
   板子一直在 `00:14.0`。按第 0 节，125us 台阶是控制器的 NAK 重试策略，两块不同的硅
   没理由策略相同。**成本是拔一次插头，而它可能推翻第 0 节和第 10 节的前提。**
2. **`iommu.passthrough=1`。** 现在是 `DMA-FQ`：每个 URB 都要 `dma_map_single`/`unmap`，
   而 unmap 是**攒起来批量刷 IOTLB** 的——吞吐友好，但那是个不受控的周期性抖动源。
   代价是丢掉 DMA 保护，对专用控制机可以接受。
3. **libusb 零拷贝缓冲**（`libusb_dev_mem_alloc()`，走 `USBDEVFS_ALLOC_MEMORY` mmap）。
   绕开 per-URB 的 `dma_map` 和用户/内核拷贝。和上一条是同一个成本的两种消法；
   现在 `host/src/transport/usb/usb.cpp` 用的是普通缓冲。
4. **xHCI 中断合并（IMOD）。** Linux 给 xHCI 设的默认节流是 **40us**
   （`xhci->imod_interval`，写进 Interrupter 的 IMOD 寄存器）。1kHz 下中断间隔 1ms，
   不该有影响；但**高帧率压测和多板共用一个 Interrupter 时会咬**。`xhci_hcd` 没有对应的
   模块参数（DT 上才有 `imod-interval-ns`），运行时改需要 root mmap
   `/sys/bus/pci/devices/<ctrl>/resource0` 后写 Runtime 区的 IMOD。
> **已出队：** 本节原有的第 5 条"隔离一个 P 核而不是 E 核，并把 SMT 兄弟一起隔离"
> 已于 2026-08-04 实测完成，结果搬到 **1.3**（p99.9 约 -5~13us，p50 不动、p99 证据不足）。

---

## 6. EtherCAT 侧

> **状态：历史记录。** 本节的全部内容基于一台装有 Intel `igc`（i225/i226）网卡的机器。
> **当前这台机器上没有 igc 网卡**——两个口都是 Realtek RTL8125（`r8169`），
> 也没有加载 IgH 的 `ec_*` 内核模块。下面的结论**没有被推翻，只是暂时无法在本机复现**。
> 换回带 igc 的机器时仍然适用。

| 项 | 当时状态 | 证据 | 持久化 |
|---|---|---|---|
| `isolcpus`/`nohz_full`/`rcu_nocbs` | 已设 | 未单独对照 | **是**（GRUB cmdline） |
| PREEMPT_RT 内核 | 是 | 未单独对照 | 是 |
| `sched_rt_runtime_us = -1` | 已设 | 沿用早期结论（曾把一个 ~50ms 的 max 定位到 RT 限流） | **否**——重启变回 950000 |
| 网卡 EEE off | 本来就 off | — | 是（驱动默认） |
| 网卡 `rx-usecs 0` | 设了会被冲掉 | **实测是空操作**，见下 | 否 |

### 6.1 关于网卡的两个坑（igc 专有）

**坑一：网卡调优必须在 EtherCAT master attach 之前做。** master 一起来，`ec_igc` 就接管了
网卡，它不再是 netdev，`ethtool` 直接报 `no device matches name`。

**坑二：`ethtool -C <if> rx-usecs 0 tx-usecs 0` 执行不了，而且执行了也没用。**

```
netlink error: igc: Queue Pair mode enabled, both Rx and Tx coalescing controlled by rx-usecs
netlink error: Invalid argument
```

igc 是 queue-pair 模式，只能写 `rx-usecs`。更关键的是：**设成 0 之后启动 master、再停掉
master 重新读，值又回到了 3**——`ec_igc` 接管网卡时会自己重新初始化硬件，ethtool 写进去的
值根本没进 EtherCAT 数据面。**不要因为"设了 rx-usecs=0"就认为网卡侧已经调过了。**

---

## 7. USB 代码本身还有没有余量（host + device 逐项）[核查 2026-08-01]

**结论：两侧都已经贴在"微帧"这个上界上，没有能改 p50 的代码改动。**
判据是实测分布本身：`min 92.0 / p50 124.8` —— 板子最快 92us 就能把回复准备好，
p50 之所以是 124.8，是**回复错过了当前微帧要等下一个**，不是代码慢。

**设备侧（固件，`app/src/usb/vendor.hpp`）**

| 项 | 状态 |
|---|---|
| 零拷贝直发 | 已实现。`CFG_TUD_VENDOR_TX/RX_BUFSIZE = 0`，不过 FIFO，`tud_vendor_n_write` 直接 claim 端点起传输 |
| 变长 + 短包语义 | 已实现。有多少发多少；短包结束传输；满 512 时补 ZLP |
| 端点大小 | HS bulk 512 字节 |
| 一次只能有一个 IN 在飞 | 是栈/硬件属性。**对请求-响应型负载无影响**——同一时刻本来就只有一个回复要发 |
| 从 ISR 直接踢泵（不等主循环） | **已实现且已实测**：p50 **零收益**，只改善尾部。原因就是上界是微帧不是"武装延迟" |
| 小瑕疵 | `tud_vendor_n_write()` 的返回值只在 `assert_debug` 里检查（release 是空操作）。当前配置下不可达，但确实是个未检查的返回值 |

**主机侧（`host/src/transport/usb/usb.cpp`）**

| 项 | 状态 |
|---|---|
| IN 队列深度 | **16 个 transfer 常驻在飞**（`kReceiveTransferCount`），管道不会空 |
| OUT 池深度 | **64 个**（`kTransmitTransferCount`），1kHz 控制率下不可能耗尽 |
| 重新提交时机 | 在完成回调里，不回主循环 |
| ZLP | `LIBUSB_TRANSFER_ADD_ZERO_PACKET` |
| 事件线程绑核 + RT | `thread_setup` 钩子 |
| **唯一的结构性问题** | `usb_receive_complete_callback()` 里是 `receive_callback_()`（完整反序列化 **+ 你的用户回调**）**先执行**，然后才 `libusb_submit_transfer()`。**如果 PID 跑在上位机，它就是在 libusb 事件线程上执行的**，和 USB 事件处理串行。16 个 transfer 在飞所以不会饿死管道，但**你的 PID 执行时间会原样叠进这条线程的响应延迟与抖动**。 |

**最后一条是唯一值得动的**，而且它不是 USB 的问题，是应用架构问题：把重计算从回调里挪出去
（回调只入队，PID 在自己的线程算），或者接受它并把 PID 做得足够短。
注意**不能简单地"先 resubmit 再处理"**——buffer 还在被读，重新提交会覆盖它，
要改就得配双缓冲或多一次拷贝。

---

## 8. 板端 C++ 全流程还有没有优化空间 [实测 2026-08-01]

**结论分两半，不要混用：**
- **USB 路径：没有有意义的空间**，板端占整个 RTT 约 2-3%（本节以下内容）。
- **EtherCAT 路径：结构上有（量子小 5 倍），但已试过的一项没兑现。** EtherCAT 的 RTT 严格
  量化到 25.75us 的周期，USB 量化到 125us——同样是赌边界，**格子小 4.85 倍**。
  但按该推理做的第一项（把 MCAN 驱动搬进 ILM）**实测零收益**。

### 8.1 主循环周期：0.72us（空闲）/ 0.85us（满载）

给固件主循环加了迭代计数器（`LIBRMCS_CAN_DIAG` 遥测里的 `loop=` 字段）实测：

| 负载 | 每 100ms 迭代数 | 周期 |
|---|---|---|
| 空闲 | 138838 | **0.72 us** |
| 双流 8000 f/s（合计 16000） | 117993 | **0.85 us** |

主循环里排着 `tud_task()`、4 个 CAN 看门狗、USB 泵、跨核泵、UART 泵、诊断泵——
**全部跑完一圈不到 1 微秒**。这一个数就把下面这类优化全部否掉了：

| 想法 | 最多能省 |
|---|---|
| 把 `tud_task()` 搬进 USB ISR | **< 0.85 us** |
| 重排主循环、把泵提前 | **< 0.85 us** |
| 把 SDK 的 MCAN 驱动从 XIP flash 挪进 ILM | ISR 总共才 1-2us，只能省其中一部分 |

对照 RTT p50 124.8us、min 92.0us —— **板端整条 C++ 路径不到 RTT 的 3%**。

### 8.2 一条确实存在但不值得动的：MCAN 驱动在 XIP flash

map 文件核实（`build-swap` release）：

```
handle_downlink                      0x00001358  ILM     <- 我们的胶水
irq_handler                          0x00001a8a  ILM
mcan_read_rxfifo                     0x8002bbb0  FLASH(XIP)   <- SDK 驱动
mcan_transmit_via_txfifo_nonblocking 0x8002bcf6  FLASH(XIP)
```

`can.cpp` 的注释早就写明了这个取舍（"Leaf calls into the MCAN driver stay in FLASH"）。
理论上可以做，但按上面的账，收益在 1us 以内，**不值得为它引入一个定制链接脚本**。

### 8.3 参考：一个 3us 的改动能做什么

CAN-FD 从 5 提到 6 Mbit（省约 3us 线上时间）时，p50 五轮为
124.4 / 105.3 / 100.6 / 100.6 / 100.3——**要么约 100 要么约 124**。
说明在微帧边界附近，3us 确实可能翻掉一整档 25us，**但完全不可控**。
所以"抠板端几微秒"不是没有效果，而是**效果是随机的**，不能作为工程手段。

---

### 8.4 固件构建类型：`debug`(-Og) 对 `release`(-O3) 值一整个微帧 [实测 2026-08-04]

**结论：这是本文档记录过的所有旋钮里最大的一个，比主机侧全部调优加起来还大。**
两块 5321 DualCan，只换固件构建类型，主机设置完全不动：

| `dual_board_test latency 0 20000`，间隔 1000us | min | p50 | p90 | p99 | p99.9 | avg | max |
|---|---|---|---|---|---|---|---|
| **`debug`(-Og)** 不绑核 | 85.4 | 120.9 | 122.2 | 129.1 | 177.6 | 119.6 | 245.0 |
| **`release`(-O3)** 不绑核 | 81.8 | **100.1** | 121.3 | 126.9 | 176.4 | 104.6 | 458.0 |
| `debug` a=7 b=6 | 85.4 | 117.6 | 121.8 | 123.3 | 132.7 | 111.7 | 161.6 |
| **`release` a=7 b=6** | 79.3 | **98.9** | 119.6 | 122.2 | 129.4 | 100.6 | 161.0 |
| `debug` a=8 b=9 | 84.6 | 120.7 | 121.9 | 125.0 | 136.7 | 115.4 | 153.3 |
| **`release` a=8 b=9** | 76.9 | **99.5** | 116.0 | 122.8 | 130.1 | 99.8 | 154.3 |

**p50 一律降约 19-21us，min 降 5-8us**，四种绑核方式下都成立。对比第 1 节：`governor` 值
16-18us 的板级 p50，**换构建类型值 20us，且不需要任何运行期配置。**

三件事：

1. **它顺带解释了 9.1 那组数为什么对不上。** 9.1 记录的 p50 是 102.4/102.9，而板子换成
   `debug` 固件后怎么测都是 118-121。**不是 9.1 测错了，是那次测量用的是 `release` 固件。**
   板级 p50 的第一嫌疑人永远是固件构建类型，不是主机。
2. **`-Og` 是怎么进去的**：`app/CMakeLists.txt:98` 在 `CMAKE_BUILD_TYPE STREQUAL "debug"`
   时加 `-Og`。而 `firmware/rmcs_board/AGENTS.md` 给 `hpm5321_dual_can` 的示例命令用的是
   `--preset release`——**板上跑 debug 多半是意外，不是选择。**
3. **吞吐完全不受影响**：两种构建下都是每总线 19000 帧/s 无损、20000 帧/s 丢约 0.67-0.69%，
   `corrupt=0`。**上限是 CAN-FD 线速，不是 CPU**，所以优化等级动不了它。

> **烧录前必须核对板级变体。** `hpm5321` 的 USB PID 是 `0xA901`，`hpm5321_dual_can` 是
> `0xA902`，而 `BOARD` 的默认值是 `hpm5321`。**不带 `-DBOARD=hpm5321_dual_can` 编出来的
> 镜像烧上去会改掉 PID 并配错引脚。** 仓库里 `firmware/rmcs_board/build/` 这棵树当前就是
> 默认的 `hpm5321`，别直接拿它烧 DualCan 板。
>
> ```bash
> cmake -S firmware/rmcs_board -B <build> -G Ninja \
>       -DCMAKE_BUILD_TYPE=release -DBOARD=hpm5321_dual_can
> cmake --build <build>
> dfu-util -d 0xa11c:0xa902 -S <serial> -a 0 \
>          -D <build>/app/output/rmcs_board_app_hpm5321_dual_can.dfu
> ```
>
> 两块板同时在线时用 `-S <serial>` 逐块烧（`dfu-util -l` 列出序列号）。
> **版本串区分不出构建类型**——它来自 `git describe`，`debug` 和 `release` 完全一样。

---

### 8.5 板端固件不在关键路径上：流水线步进 = CAN 线时 [实测 2026-08-04]

**结论：稳态下每帧步进 51.7-51.8us，而 CAN-FD 8 字节帧的线时是 50.3us——差 1.4us。
也就是说两块板的固件周转加上 USB 上行串行化，合计只有约 1.4us 没有被线时掩盖。
板端优化的余量到此为止。**

方法（`host/examples/can_path_split.cpp`）：把 N 帧塞进**一个** USB 包发给 A 板，
逐帧记录它们从 B 板回到主机的时刻。USB 下行只付一次，所以只体现在第 0 帧；
**任何随位置增长的东西都发生在包落到板上之后**。

| burst | pos0 p50 | pos0 min | 末帧 p50 | 步进（均值） |
|---|---|---|---|---|
| 8（2000 次） | 111.3 | 84.0 | 474.2 | **(474.2-111.3)/7 = 51.8us** |
| 16（1500 次） | 111.7 | 87.4 | 887.1 | **(887.1-111.7)/15 = 51.7us** |

两个 burst 长度给出同一个步进，且与 9.3 独立测到的线速上限自洽
（19870 帧/s = 50.3us/帧）。

三件事：

1. **固件被完全流水线化。** 步进只比线时多 1.4us，说明 A 板的解析+MCAN 写入、
   B 板的 ISR+序列化、以及 USB 上行，全都藏在了前一帧的线时里。
   **想在板端 C++ 上省时间，天花板是 1.4us。**
2. **逐帧的 ~45/~59 交替不是抖动**，是上行帧被成对打进同一个 USB IN 事务，
   取均值才有意义。单看相邻两帧的差会得出错误结论。
3. **固定下限（第 0 帧）才是非 CAN 部分。** pos0 的 min 84.0/87.4 减去线时 50.3
   得到约 **34us**，与 1.2 节里 `dual_board_test` 最好的一次 min 76.9（对应 26.6us）
   同一量级。这段是 USB 下行 + 主机唤醒 + 两次固件的固定开销，**只付一次**。

#### 8.5.1 固定下限的分布是双峰的，峰间距约 14us——不是 125us 的微帧格子 [实测 2026-08-04]

`can_path_split 4 6000`，第 0 帧相对本轮最小值的直方图（2us 一格，min=83.0us）：

```
  +4 us    740  ###############           <- 簇 1
 +18 us   2396  ##################...##   <- 簇 2，占 40%，p50 落在这里
 +32 us    198  ####                      <- 微弱的第三簇
 尾部平坦延伸到 +42
```

**整个分布只跨 42us，三分之一个微帧。所以第 0 节"p50 被量化到 125us"的说法
描述不了这条尺子。** 实际形态是双峰，峰间距约 14us。三个后果：

1. **它解释了本会话反复出现的"同配置 p50 在 ~101 和 ~116-121 之间跳"**——p50 落在
   哪一簇取决于哪一簇过半，所以**单轮 p50 在这台机器上不是可靠的比较量**，
   必须看 min、avg 或多轮。1.3 和 8.4 的对照都因此改用了 min/avg。
2. **`+4` 簇的存在证明硬件能快 14us**，也就是说这 14us 在原理上是可取的。
   但**是什么决定落进哪一簇，尚未查明**。
3. **已排除：xHCI 中断线程优先级不是原因。** FIFO 90 对 99，p50 107.8 对 105.8
   ——2us，在轮间噪声内 `[实测无效]`。

#### 8.5.2 定位完成：全部方差在 USB 上行，CAN 段几乎无抖动 [实测 2026-08-04]

**不需要改固件**——协议的上行帧本来就带 B 板的硬件 TSU 时间戳（`CanDataView::timestamp_us`，
1 tick = 1us）。板子时钟和主机时钟各自自由运行，绝对差没有意义，但**在同一个 burst 内比较
就没有漂移问题**：相邻两帧只隔约 51us，两个时钟来不及漂开。

  - `board_step` = B 板 TSU 看到的帧间隔（纯 CAN 段）
  - `host_step`  = 主机看到的帧间隔（CAN 段 + 上行段）
  - `divergence` = `host_step - board_step`（**只剩上行段**）

`can_path_split 8 5000`，35000 个相邻帧对 `[实测]`：

| | min | p50 | p90 | max | 分布 |
|---|---|---|---|---|---|
| **CAN 段** | 51.0 | 52.0 | 52.0 | **53.0** | 33347 个在 +51us，1653 个在 +53us |
| **上行段** | -49.6 | -7.0 | +13.2 | +234.9 | 双峰：**-10us 占 40%**、**+12us 占 18%** |

**结论：CAN 段总扩散 2us、95% 的样本落在同一格；全部时序方差都是在 B 板打完时间戳之后
引入的，也就是 USB 上行加主机唤醒。**

发散为负表示主机看到的两帧比线上更靠近——**两帧被打进了同一个 USB IN 事务**；为正表示被
拉开——**这一帧没赶上，等了下一次 IN 机会**。这就是 8.5.1 那两个簇的来源。

> **注意适用范围。** 本测量里相邻帧只隔 51us，比 1kHz 控制环的 1ms 密得多，所以"两帧挤进
> 一次 IN"这个现象在真实占空比下不会出现。**但 8.5.1 的 pos0 分布是在 2ms 空闲之后测的、
> 同样双峰**，说明"等下一次 IN 机会"这一半在 1kHz 下依然成立。

**这把剩余余量的性质定死了：它不在 CAN、不在固件、不在拷贝，而在
"设备已经准备好数据到主机的 bulk IN 事务真正取走它"之间。** 而这段是
**主机控制器对 NAK 中 bulk 端点的重试策略**，Linux 没有暴露任何旋钮
（第 5 节第 4 条的 IMOD 只影响中断合并，不影响事务调度本身）。所以：

- **换控制器可能改变它**（第 5 节第 1 条），但本机雷电控制器的 USB2 端口物理未接线；
- **换传输可以绕开它**——EtherCAT 是确定性调度，没有 NAK/重试相位。仓库已有 IgH 后端，
  第 10 节正是讲这两种传输为什么对主机调优敏感度不同。`[推断，未在本机复现]`

> **推论：剩下的优化余量在 CAN 位定时上，不在主机也不在固件。**
> 每帧 50.3us 里约 30us 花在 1 Mbit/s 的仲裁段。仲裁段提到 2M 约值 -15us、
> 数据段 5M->8M 约值 -7us `[推断，未实测]`。但 `can.hpp` 的注释记着采样点从 87.5%
> 偏到 75% 曾让 mc02 完全不 ACK 本板的 FD 帧（PSR.DLEC = ACK error，TEC 进 error-passive），
> **所以提波特率必须重算采样点并两端对齐，不是改个数字。**

---

## 9. 多块 USB 板并发：会互相拖累，且必须调中断线程优先级 [实测 2026-08-01]

两块板（HPM5321 DualCan + HPM6E8Y）同时跑同一个延迟测试，各自绑不同的核。
两块都在同一个 xHCI 控制器的同一个 root hub，因此**共用一个中断线程**
（RT 内核默认 FIFO 50）。

| | 5321 单独 | 5321 并发 | 6E8Y 单独 | 6E8Y 并发 |
|---|---|---|---|---|
| p50 | **99.8** | **124.6** | 124.9 | 124.7 |
| max | 130.3 | **975.5** | 160.6 | **1098.0** |

**两件事同时发生：**

1. **5321 掉了整整一个微帧**（99.8 -> 124.6）。两个设备共享同一份调度，回复不再能稳定
   赶上早一班。**单板时那 25us 的优势，在有第二块板的瞬间消失。**
2. **两块板的 max 都炸到约 1 毫秒**，是单板的 6-7 倍。对 1kHz 控制环这等于**整拍丢失**。

把共享的中断线程提到 FIFO 90 之后：

| | 并发 + IRQ FIFO 50 | 并发 + IRQ FIFO 90 |
|---|---|---|
| 5321 max | 975.5 | **180.3** |
| 6E8Y max | 1098.0 | **177.2** |
| p50（两者） | 124.6 / 124.7 | 124.7 / 124.8 |

**尾部救回来了（约 5.5 倍），p50 救不回来。** 这修正了第 2 节表里的那条结论：
单板时提高 xHCI 中断线程优先级确实无效，**但多板时它是必需的**。

### 9.1 复测：单条串行链路上它值多少 [实测 2026-08-04]

两块 5321 DualCan 同挂 `00:14.0`（共用 IRQ 152），`dual_board_test latency` 间隔 1000us，
`governor=performance`，交替两轮：

| 共享 IRQ 线程优先级 | p50 | p99.9 | max |
|---|---|---|---|
| FIFO 50（RT 默认） | 102.1 / 102.5 | 128.4 / 142.3 | **189.9 / 222.3** |
| FIFO 90 | 102.4 / 102.9 | 125.0 / 135.9 | **126.3 / 141.4** |

**max 降了约 60-80us，p50 和 min 完全不动**——和上面那组"尾部救得回来、p50 救不回来"
一致。**但没有复现约 1ms 那一档**，原因是测试形态不同：上面那组是两块板**各跑一个独立的
测量循环**（两条链路同时压中断线程），9.1 是**一条 A->B 串行链路**，同一时刻只有一笔事务在飞。
**所以约 1ms 那个数是"两条并发链路"的量级，不是"插了两块板"的量级。**

`host-tuning.sh` 现在会自动数出同一控制器上挂了几块板，只在 >= 2 时把该线程提到 FIFO 90。

> **多板部署的硬性要求**：
> 1. 把该控制器的 xHCI 中断线程提到 FIFO 90。并发越多越关键（并发两条链路时是 max 约 1ms
>    -> 180us，单条串行链路时是 190 -> 126us）。
>    **IRQ 号不要抄文档**——`host-tuning.sh` 会发现它并自动处理；
> 2. 不要指望多板还能拿到单板的 p50——**同一控制器上的板越多，越是都退化到微帧边界**；
> 3. 想避开共享调度只能把板分到**不同的 xHCI 控制器**上（本机有 `00:14.0` 和 `00:0d.0`），
>    但控制器数量有限，扩不到很多块。

---

### 9.2 USB HS 的包率上限：单板约 56000 包/s，且是**每设备**的 [实测 2026-08-04]

**结论：`dual_board_test stress` 那个 19000 帧/s 是 CAN 线速，跟 USB 没关系。USB 侧还有
约 3 倍余量，而且加板子能线性叠加。** 工具是 `host/examples/usb_packet_rate.cpp`
（`release` 固件，两块 5321 DualCan）。

**为什么以前看不出来**：`stress` 把 can0 和 can1 batch 进**同一个** USB 包，所以
19000 帧/s/总线只产生 19000 包/s——包率恰好等于帧率，两个上限被混在一起了。
`usb_packet_rate` 把每条记录拆进各自的包，包率就能在不动 CAN 帧率的前提下抬上去。

| 配置（flood，无节流） | 包/s | 同期 CAN 收到 |
|---|---|---|
| 单板 `combined`（1 包 = 2 条 CAN 记录） | 43198 | 19205 / 19580 f/s |
| 单板 `split`（1 包 = 1 条记录） | **56231** | 19204 / 19580 f/s |
| 单板 `split3`（+UART 各占一包） | **58715** | 19204 / 19572 f/s |
| **双板同时 `split`** | **105157**（A 52580 + B 52577） | 见下方警告 |
| 双板同时 `split3` | 106339 | 见下方警告 |

定速验证（无丢失，精确交付）：

| | 包/s | bus0 | bus1 | uart |
|---|---|---|---|---|
| `split` 19000 | 38000 | 19000 f/s | 19000 f/s | — |
| `split3` 19000 | **57001** | 19000 f/s | 19000 f/s | 91067 B/s |

四件事：

1. **CAN 帧率与包率完全解耦。** `split` 把包率从 19000 翻到 38000，每总线仍然精确交付
   19000 f/s。**CAN 那 19870 f/s 的墙和 USB 无关**（线速核算与证据见 9.3）。
2. **上限是 USB 路径，不是主机。** flood 期间发送线程只占 **23-24% CPU**、事件线程 15-19%
   ——循环阻塞在等 `acquire_transmit_buffer()` 的条件变量（64 个 transfer 的池由完成回调
   归还），不是 CPU 打满。
3. **上限是"每设备"的，不是控制器共享的。** 两块板同挂 `00:14.0`，各自 flood 时总量
   **105157 包/s，接近翻倍（1.87x）**，每块只掉约 7%（56231 -> 52580）。
   **所以多插板子确实能增加 USB 侧的总包率**，不像共享中断线程那样纯粹是负担。
   **能不能继续线性扩到第 3 块板未测**——本机只有两块 `a902`。
4. **`combined` 反而比 `split` 慢**（43198 vs 56231 包/s）。每包多带一条记录并没有换来
   更高的包率，**说明瓶颈在每包的固定开销上，不在字节数**（11 B 对 22 B 在 480 Mbit/s
   上差 0.18us，可以忽略）。要提总吞吐应该往"每包塞更多记录"的方向走，而不是拆包。

> **双板 flood 那两行的 CAN 数字不能用。** 本台架 A.CAN0 和 B.CAN0 是**同一根线**，
> 两块板同时发就是在同一条 CAN 总线上互相仲裁，收到的自然塌到约 950 f/s。
> **USB 包率数字仍然有效**——那些包确实被 USB 交付到了设备，之后固件拿它们怎么办
> （MCAN TX FIFO 满了就丢）是另一回事，单板 flood 那行也一样：推 28104 f/s/总线、
> 只有 19204 f/s 上得了线，而 USB 照样跑满 56231 包/s。

---

### 9.3 CAN 吞吐的真正上限：线速，不是 USB 也不是 CPU [实测 2026-08-04]

**结论：每条总线 CAN-FD（1M 仲裁 / 5M 数据、8 字节）无损上限 19870 帧/s，classic CAN
（1M、8 字节）8560 帧/s。这是 CAN 物理层的帧时间，主机调优、固件优化等级、USB 侧统统动不了它。**

三条独立证据，任何一条单独看都可能被误读，合起来是确定的：

**证据一：收到的量在某个值上焊死，推多快都一样。** `dual_board_test stress <rate> 8 1`
（单总线，CAN-FD）：

| 目标速率 | 8 秒实收 | 折算到达速率 |
|---|---|---|
| 19000 | 152000（全收） | 19000 |
| 20000 | 158957 | **19870** |
| 22000 | 158955 | **19869** |
| 25000 | 158975 | **19872** |
| 30000 | 159014 | **19877** |

**推 30000 和推 22000 收到的一样多**——典型的介质饱和，不是调度抖动。

**证据二：单总线上限 = 双总线同跑时的单总线速率。** 单总线 19870，双总线同跑时
bus0 19865 / bus1 20000。**若瓶颈是共享资源（USB、中断、CPU），只跑一条时它必须更快，
但它没有。** 反过来说，一块板的 USB 链路已证明能同时扛住约 39900 帧/s，是单总线上限的 2 倍。

**证据三：换 classic CAN，上限按线速比例掉下去。** `RMCS_CAN_CLASSIC=1`，单总线：

| 目标速率 | 8 秒实收 | 折算 |
|---|---|---|
| 8000 | 64000（全收） | 8000 |
| 9000 | 68294 | **8537** |
| 12000 | 68431 | **8554** |
| 20000 | 68527 | **8566** |

**USB 那边什么都没变，所以变的不可能是 USB。** 上限跟着 CAN 位定时走。

核对 CAN 规范（标准 11 位 ID、8 字节数据）：

| | 实测上限 | 折算每帧 | 按规范算 |
|---|---|---|---|
| CAN-FD 1M/5M + BRS | 19870 帧/s | **50.3 us** | ~49 us（仲裁段 17 位 @1M + 数据段约 95 位 @5M + 收尾 13 位 @1M） |
| Classic CAN 1M | 8560 帧/s | **116.8 us** | ~121 us（约 111 位 + 填充位 @1M） |

两者都落在规范值 3% 以内，**比值也对得上**（实测 2.32x，线速算出来 2.42x）。

**一个结构性限制，比加板子更值得注意**：`core/src/protocol/serializer.hpp` 的
`required_can_size()` 硬性要求 `can_data.size() <= 8`，而 DLC 字段只有 3 位
（`data_length_code = length - 1`）——**协议在结构上就只能表示 1-8 字节，用不了 CAN-FD
的 64 字节负载**。后果：

- 每帧 50.3us 里约 30us 花在 1 Mbit/s 的仲裁段（固定开销），只有约 19us 用在 5 Mbit/s 数据段。
- **有效载荷带宽 = 19870 × 8 B ≈ 155 KB/s，而数据段的物理能力是 625 KB/s——效率约 25%。**
- 若协议支持 64 字节：每帧约 138us → 7250 帧/s，但 **7250 × 64 B ≈ 464 KB/s，约 3 倍**。

这是设计取舍不是缺陷（8 字节定长把协议头压到 3 字节、解析无分支）。

**而且真实代价比"扩个字段"大得多——它会把 CAN 控制器的 FIFO 深度砍掉 4 倍。**
协议头这边确实有空位（`CanHeaderStandard` 的 bit 20 空着，`DataLengthCode` 从 3 位扩到
4 位不用加长 3 字节的头），但**控制器那边的元素尺寸是按最大负载配的，与实际 DLC 无关**：

| | 现在（8 字节元素） | 若支持 64 字节 |
|---|---|---|
| 每元素 RAM（`MCAN_TXRX_ELEM_SIZE_*`） | **16 B**（8 头 + 8 负载） | **72 B**（8 头 + 64 负载） |
| SDK 默认 RX FIFO / TX BUF 深度 | 32 / 32 | **8 / 8** |
| 突发容限 @19870 帧/s | 32 x 50.3us ≈ **1.6 ms** | 8 x 50.3us ≈ **0.4 ms** |

**这不是 STM32 的毛病，两边是同一个 Bosch M_CAN IP。** HPM5300 系列还有
`MCAN_SOC_MSG_BUF_IN_AHB_RAM = 1`——**所有 MCAN 实例共用一块 AHB SRAM**，
所以 DualCan 板上两条总线是同一个预算，扩容要两条一起吃 4.5 倍。

**本仓库两块板都已经有意避开了这一点**，`firmware/rmcs_board/app/src/can/can.hpp`：

```cpp
// Keep the default 8-byte element size even for CAN-FD: the frames on this
// bus never exceed 8 data bytes, so RAM usage stays identical to classic CAN.
config.ram_config.txbuf_fifo_or_queue_elem_count = MCAN_TXBUF_SIZE_CAN_DEFAULT;  // 32, not 8
```

控制器**常驻 FD 模式**（"a strict superset"），帧类型逐帧由 `is_fdcan` 选，
于是拿到的是 **FD 的速度 + classic 的 FIFO 深度**。`mc02` 的 CubeMX 配置同理
（`RxFifo0ElmtSize = FDCAN_DATA_BYTES_8`、`RxFifo0ElmtsNbr = 32`）。外部节点发来的
DLC > 8 的 FD 帧由 `can.cpp` 显式丢弃并继续排空，因为硬件已经把它截断了。

**这个深度不是余量充足，是已经撞过的。** `can.cpp` 的发送路径注释记着：USB 按微帧
成批交付，"a burst can exceed 32 even when the average rate is well under bus capacity"，
当年直接写寄存器、满了就放弃的做法丢过整批，唯一的迹象是一个青色 LED——所以才加了
软件队列垫在 FIFO 后面。**32 都不够用，8 会差得多。**

> **它影响的是突发容限和丢帧，不是延迟。** p50 完全不动——一帧走的路径与 FIFO 深度无关，
> FIFO 只在到达速率短暂超过排空速率时才起作用。间接地，FIFO 浅 4 倍意味着 ISR 的服务窗口
> 从 1.6 ms 缩到 0.4 ms，对 ISR 抖动的容忍度低 4 倍，但表现是"更容易丢"而不是"每帧更慢"。

**判据：大部分帧是 8 字节时，现状就是最优解，不要动。** 扩 DLC 只有在同时满足三条时才值得：
确实有大批量字节要搬、能把大帧与实时小帧**分到不同总线**、且能腾出 4.5 倍 AHB SRAM。
到那一步时，**加一条专用总线通常比改协议更划算**。若瓶颈只是帧率（每帧一条电机指令），
扩 DLC 一点用没有：19870 帧/s 是这套位定时下的硬上限，只能提数据段波特率或加总线。

USB 侧每帧的实际字节数（`required_can_size()` 的算式
`(field_header + can_header - 1) + data + timestamp`，`FieldHeader` 1 B、
`CanHeaderStandard` 3 B）：

| 方向 | 组成 | 每帧 |
|---|---|---|
| 下行（主机->板，无时间戳） | 1 + 3 - 1 + 8 | **11 B** |
| 上行（板->主机，带硬件时间戳 4 B） | 1 + 3 - 1 + 8 + 4 | **15 B** |

> **`dual_board_test stress` 原有三个 bug，2026-08-04 已修，但要知道它们长什么样**——
> 旧的运行记录如果 `per_packet > 1`，数字全部不可信。三个都出自同一个根源：
> 循环把"一次迭代"和"一帧"当成了一回事。
>
> 1. **第 4/5 个参数是 `buses` 和 `per_packet`，不是核号**，而 usage 字符串里没写
>    （只写了 `stress [frames_per_sec] [seconds]`）。把核号传进去会变成"7 条总线、
>    每包 6 帧"。**已修**：usage 补全，并加了 `buses` 1..2、`per_packet >= 1` 的守卫。
> 2. **`payload` 每次迭代只算一次，内层 `repeat` 循环发的是 `per_packet` 份完全相同的帧。**
>    接收端按序列号连续性判丢包，于是每组里 `per_packet - 1` 帧被误判成 gap ——
>    **报出来的"丢包"根本没发生过**。**已修**：序列号按帧推进。
> 3. **`total_per_bus` 漏乘 `per_packet`，节流也按迭代而非按帧**，所以 `sent` 少算
>    `per_packet` 倍、实际帧率是请求值的 `per_packet` 倍，丢包率做无符号减法后下溢，
>    打印出 `23058430092136792%` 这种数。**已修**：周期改为 `per_packet / frames_per_sec`，
>    总帧数与参数名一致。
>
> 修复后的回归验证（16000 帧/s/总线、8 秒、两条总线）：`per_packet` 取 1 / 4 / 8
> 都是 `sent 128000/bus`、`received 128000`、`gaps 0` `[实测 2026-08-04]`。
>
> 本节全部数据都是用 `per_packet = 1` 跑的，不受这三个 bug 影响。

---

## 10. 为什么两种传输对主机调优的敏感度不同

理解这一点比记住清单更有用。

**USB 的 p50 被微帧量化成一格一格的。** HS 微帧是 125us；实测 min 92.0、p50 124.8、
p90 126.3——绝大多数落在一个微帧上，偶尔赶上早一班就是 92。所以：

- **量化是门槛，不是屏蔽。** 曾经写在这里的"主机调优只能改善尾部、改不了板级 p50"
  **是错的，已于 2026-08-04 实测推翻（见 1.2）**：主机路径省下的 10us 若刚好让回复赶上
  当前微帧，收益就不是 10us 而是**一整格**——实测板级 p50 从 120 掉到 102。
  反过来，低于门槛的改动（板端抠几微秒）效果是随机的，见 8.3。
- **它是否兑现，取决于你的循环让不让核空闲。** 紧凑压测下主机调优确实一点用都没有（1.1），
  1kHz 控制环下值 16-18us（1.2）。**先确认占空比，再看结论。**
- **另一条改板级 p50 的路是减少板端周转时间**，让回复赶上早一个微帧。这同样是全有全无的
  一档（25us），已知办法是不释放 core1（单核镜像 p50 99.8）。见
  [firmware/rmcs_board/AGENTS.md](firmware/rmcs_board/AGENTS.md) 的选型表。

**EtherCAT 对主机电源管理不敏感，因为它忙轮询。** 但它对**主机栈本身**敏感：实测周期
25.75us 里只有约 8us 是线上时间，其余约 18us 是 IgH 协议栈 + 网卡往返。所以 EtherCAT 侧
想再快，动的是主机栈和隔离，不是网卡参数。

**两者的失效模式不同，选型时看 p50 会选错**：USB 是"要么快要么很慢"（p99-p50 = 33us），
EtherCAT 是"稳定地慢一点"（p99-p50 = 7us）。完整三档分布见
[firmware/rmcs_board/ecat/DESIGN.md](firmware/rmcs_board/ecat/DESIGN.md) 3.5 节。
